export const disabledElement = 'opacity-30 cursor-not-allowed pointer-events-none select-none';

export const buttonElement = `inline-flex items-center justify-center space-x-2 py-2 px-4 border border-${window.colors.primary200} bg-${window.colors.primary150} hover:bg-${window.colors.primary200} focus:outline-none focus:ring-4 focus:ring-gray-${window.colors.textNormal} rounded-3xl transition-colors duration-150`;

export const tabPillElement = `py-2 px-4 text-xs cursor-pointer rounded-3xl bg-${window.colors.primary150} hover:bg-${window.colors.primary200}`;

export const labelPillElement = `bg-${window.colors.primary200} text-${window.colors.textNormal} text-sm font-medium me-2 px-2.5 py-0.5 rounded-full`;

export const linkElement = 'font-semibold hover:underline';
